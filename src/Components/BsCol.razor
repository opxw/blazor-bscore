<div class="@GetClass()">
	@ChildContent
</div>

@code
{
	[Parameter]
	public RenderFragment? ChildContent { get; set; }

	[Parameter]
	public GridSizeKind SizeKind { get; set; } = GridSizeKind.Auto;

	[Parameter]
	public int Size { get; set; } = 12;

	string GetClass()
	{
		var result = $"col-{Size}";

		if (SizeKind != GridSizeKind.Auto)
		{

		}

		return result;
	}
}